dist: trusty
sudo: false
language: python

git:
  depth: false

python:
  - '3.6'

install:
  - pip install invoke
  
script:
  - git rev-parse HEAD
  - git rev-parse --verify HEAD
  - touch test.foobar
  - git add -A
  - git commit -m "Add test.foobar"
  - git push origin HEAD:master

